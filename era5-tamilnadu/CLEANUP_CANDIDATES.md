# Cleanup Candidates — Tamil Nadu Pipeline

Generated by an audit pass (see `CHANGELOG.md` v3.2 and
`FYP_Tamil_Nadu_Phase_Audits_Consolidated.md`). This is a review list
only — **nothing here has been deleted**. Verify each item yourself
before removing it; sizes are approximate.

| # | Path | Size | What it is | Recommendation |
|---|---|---|---|---|
| 1 | `Objective 1/` | 860 KB | A full duplicate of the pipeline scripts, but an **older snapshot**: it has 25 `.py` files and is missing `10_physics_validation.py` and `11_level_b_seasonal_analysis.py` entirely. Its own `07_feasibility_filter.py` predates the corrosion-veto/safety-exclusion filters, and its `NEXT_STEPS.md` literally says physics validation is "a stretch goal, not written" — confirming it predates the completed root-level pipeline. | **Safe to delete** once you've confirmed you don't need it as a historical snapshot. Keep only if you specifically want a record of the pre-Phase-7 state. |
| 2 | `data - Copy/` | 134 MB | An earlier backup of `data/processed/` + `data/preprocessed/` + `data/plots/`, taken **before** Phase 7 (physics validation) and Phase 8 (recommendation cards) existed — it has clustering/MCDM/PCM outputs but no `physics_validation_*.csv`, no `recommendation_cards.md`, no `level_b_*` files. The live `data/` folder (13 GB, includes raw ERA5/NASA POWER downloads) is the current, complete, correct one. | **Safe to delete** if you don't need this specific intermediate snapshot as a fallback. It contains no raw downloads, so deleting it does not affect re-running anything. |
| 3 | `docs/era5_tamilnadu_1.zip` | ~64 KB | An older duplicate of `docs/era5_tamilnadu.zip` — missing `23_PLOTS_GUIDE.md`, and not the one I extracted/updated this session. | **Safe to delete.** `docs/era5_tamilnadu.zip` (updated to v3.2 this session) is the current one. |
| 4 | `docs/era5_tamilnadu.zip` | ~76 KB | The zipped copy of the 24 phase-audit `.md` files. Now duplicates `docs/era5_tamilnadu/` (see next row) since I extracted it there to make the edits. | **Keep one, not both.** Either delete the zip and keep the extracted folder as the live docs (simplest going forward), or delete the extracted folder and keep only the zip as an archive — just pick one so they don't silently drift out of sync again. |
| 5 | `docs/era5_tamilnadu/` | ~400 KB | The extracted, **currently up-to-date (v3.2)** phase-audit docs — this is what I actually edited this session. | **Keep.** This is the live source; see row 4 for what to do with the zip. |
| 6 | `PCM_data/PCM_data/` (nested) | ~700 KB | A doubly-nested folder (`PCM_data/PCM_data/01_preprocess.py`, `PCM_data/PCM_data/data/*`) — almost certainly a zip-extraction artifact. The one file the live scripts actually read (`PCM_Properties_cleaned_mice_pmm_detailed.csv`) already has a correct, non-nested copy at `PCM_data/data/`. | **Probably safe to delete the nested `PCM_data/PCM_data/` subfolder**, but first confirm `PCM_data/data/PCM_Properties_cleaned_mice_pmm_detailed.csv` is up to date (it is, as of this audit) — if you ever need to regenerate it, `PCM_data/PCM_data/01_preprocess.py` is the actual preprocessing script, so don't delete it without checking you have another copy of that script. |
| 7 | `sources_extracted/sources/` | ~418 KB | Contains 29 of the 33 items in `sources/` (missing `extracted_papers/`, `pdfs/`, `references.bib`, `references - Copy.bib`) — the common files did not show as different, so this looks like a partial, older extraction of the same literature-summary set. | **Likely safe to delete** — `sources/` (288 MB, includes the actual PDFs) is the complete, current set. Do a quick visual diff yourself first since I only checked for content differences on common filenames, not exhaustively. |
| 8 | `sources/references - Copy.bib` | small | A "Copy" duplicate of `sources/references.bib` sitting alongside it. | **Compare the two and delete whichever is stale** — I did not diff their contents. |
| 9 | `__pycache__/` (root) and any nested `__pycache__/` under `PCM_data/` | ~232 KB | Python bytecode cache. Regenerates automatically the next time any script runs. | **Always safe to delete.** |
| 10 | `plots/` vs `plots_tamilnadu_ppt/` | 100 KB / 6.4 MB | Not flagged as redundant — `plots_tamilnadu_ppt/` looks like a curated, presentation-ready subset (much larger, ppt-oriented name) rather than a duplicate of `plots/`. | **No action** — just noting it in case you don't remember creating two plot folders. Verify content before deciding. |

## Not reviewed / out of scope
- `objective2_design_optimization/` (sibling directory, separate objective — not touched or assessed).
- Anything under `PCM-Selection-ML-model/` or `PCM-Selection-ML-1/` one level up — those are separate repo copies I only briefly inspected to confirm this `tamilnadu_pipeline/` folder is the live, executed one. Not assessed for redundancy.

## What I actually changed this session (for reference)
- **Code:** `10_physics_validation.py` — fixed two backward-Euler solver bugs (see `CHANGELOG.md` v3.2).
- **Regenerated data (by re-running the fixed scripts, not hand-edited):**
  - `data/processed/pcm/physics_validation_results.csv`
  - `data/processed/pcm/physics_validation_spearman.csv`
  - `data/processed/pcm/recommendation_cards.md`
- **Docs edited:** `CHANGELOG.md`, `FINALIZATION_SUMMARY.md`, `FYP_Tamil_Nadu_Enhanced_Audit.md`, `FYP_Tamil_Nadu_Phase_Audits_Consolidated.md`, and the four files inside `docs/era5_tamilnadu/` listed in row 5 above.
- Nothing else in `data/`, `PCM_data/`, or the other pipeline scripts was modified.
