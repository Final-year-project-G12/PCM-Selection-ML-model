# Cleanup Candidates — Tamil Nadu Pipeline

Generated by an audit pass (see `CHANGELOG.md` v3.2 and the
now-deleted `FYP_Tamil_Nadu_Phase_Audits_Consolidated.md` — see this
doc's own 2026-09-16 update below for why). Originally a review-only
list; **items 1-3 and 7-9 were deleted on 2026-09-16** after re-verifying
each against current disk state (all still matched this doc's description;
the two "verify/diff first" items — `sources_extracted/sources/` vs
`sources/`, and `references - Copy.bib` vs `references.bib` — were
independently re-diffed and confirmed exact/no-difference duplicates
before deletion). None of the deleted paths were git-tracked, so this was
not recoverable via `git checkout` — verification was done first for that
reason. See `CHANGELOG.md`'s 2026-09-16 entry for the full list of what
was removed. Items 4-6 and 10 below were **not** deleted (row 4's zip
duplicate was folded into that decision too — see its row).

| # | Path | Size | What it is | Recommendation | Outcome |
|---|---|---|---|---|---|
| 1 | `Objective 1/` | 860 KB | A full duplicate of the pipeline scripts, but an **older snapshot**: it has 25 `.py` files and is missing `10_physics_validation.py` and `11_level_b_seasonal_analysis.py` entirely. Its own `07_feasibility_filter.py` predates the corrosion-veto/safety-exclusion filters, and its `NEXT_STEPS.md` literally says physics validation is "a stretch goal, not written" — confirming it predates the completed root-level pipeline. | Safe to delete once you've confirmed you don't need it as a historical snapshot. | **DELETED 2026-09-16.** |
| 2 | `data - Copy/` | 134 MB | An earlier backup of `data/processed/` + `data/preprocessed/` + `data/plots/`, taken **before** Phase 7 (physics validation) and Phase 8 (recommendation cards) existed. The live `data/` folder (13 GB, includes raw ERA5/NASA POWER downloads) is the current, complete, correct one. | Safe to delete — no raw downloads inside, doesn't affect re-running anything. | **DELETED 2026-09-16.** |
| 3 | `docs/era5_tamilnadu_1.zip` | ~64 KB | An older duplicate of `docs/era5_tamilnadu.zip` — missing `23_PLOTS_GUIDE.md`. | Safe to delete. | **DELETED 2026-09-16.** |
| 4 | `docs/era5_tamilnadu.zip` | ~76 KB | The zipped copy of the phase-audit `.md` files, duplicating `docs/era5_tamilnadu/` (row 5) since it was extracted there to make edits — now badly stale after the 2026-09-16 doc pass (elevation, k=3, corrected `TM_TARGET_C`, all only in the extracted folder). | Keep one, not both — pick the extracted folder (it's what's actually maintained). | **DELETED 2026-09-16** (the zip; folder kept — see row 5). |
| 5 | `docs/era5_tamilnadu/` | ~400 KB+ | The extracted, actively-maintained phase-audit docs — updated again 2026-09-16 (elevation integration, k-selection correction, `TM_TARGET_C` fix, physics-vs-MCDM disagreement). | Keep. | **KEPT — live docs.** |
| 6 | `PCM_data/PCM_data/` (nested) | ~700 KB | The actual source-of-truth location: holds `PCM_data/PCM_data/01_preprocess.py`, the real script that generates the PCM property CSVs, plus its full output set. `PCM_data/data/PCM_Properties_cleaned_mice_pmm_detailed.csv` (what `06_build_pcm_database.py` reads) is a flat copy kept in sync with it. | Do NOT delete — it's the generator, not redundant output. | **KEPT.** |
| 7 | `sources_extracted/sources/` | ~418 KB | Contains 29 of the 33 items in `sources/` — a partial, older extraction of the same literature-summary set. | Likely safe to delete, diff first. | **DELETED 2026-09-16** — re-diffed first (`diff -rq` against `sources/` on common filenames): zero content differences found, confirming the earlier audit's read. |
| 8 | `sources/references - Copy.bib` | 20 KB | A "Copy" duplicate of `sources/references.bib`. | Compare and delete the stale one. | **DELETED 2026-09-16** — re-diffed first: byte-identical to `references.bib` (0 diff lines), so no content was lost. |
| 9 | `__pycache__/` (root) and any nested `__pycache__/` under `PCM_data/`, `plots/` | ~232 KB+ | Python bytecode cache. Regenerates automatically. | Always safe to delete. | **DELETED 2026-09-16** (root + `plots/__pycache__/`; regenerates on next run). |
| 10 | `plots/` vs `plots_tamilnadu_ppt/` | 100 KB / 6.4 MB | Not flagged as redundant — `plots_tamilnadu_ppt/` looks like a curated, presentation-ready subset. | No action — verify content before deciding. | **NOT TOUCHED** — still open, not reviewed further this pass. |

## Not reviewed / out of scope
- `objective2_design_optimization/` (sibling directory, separate objective — not touched or assessed).
- Anything under `PCM-Selection-ML-model/` or `PCM-Selection-ML-1/` one level up — those are separate repo copies I only briefly inspected to confirm this `tamilnadu_pipeline/` folder is the live, executed one. Not assessed for redundancy.

## What I actually changed this session (for reference)
- **Code:** `10_physics_validation.py` — fixed two backward-Euler solver bugs (see `CHANGELOG.md` v3.2).
- **Regenerated data (by re-running the fixed scripts, not hand-edited):**
  - `data/processed/pcm/physics_validation_results.csv`
  - `data/processed/pcm/physics_validation_spearman.csv`
  - `data/processed/pcm/recommendation_cards.md`
- **Docs edited:** `CHANGELOG.md`, `FINALIZATION_SUMMARY.md`, `FYP_Tamil_Nadu_Enhanced_Audit.md`, `FYP_Tamil_Nadu_Phase_Audits_Consolidated.md`, and the four files inside `docs/era5_tamilnadu/` listed in row 5 above.
- Nothing else in `data/`, `PCM_data/`, or the other pipeline scripts was modified.
