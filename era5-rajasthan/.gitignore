.cdsapirc

# ── Large / regenerated data (also covered by the repo-root .gitignore,
#    kept here too so this folder is self-contained) ────────────────────
# ERA5 NetCDF (old full-state grid + new population-points), NASA POWER
# JSON cache, WorldPop population raster (~1.6GB), GADM boundary GeoJSON.
data/raw/
# Combined/processed CSVs, incl. climate_rajasthan_points.csv,
# population_grid_points.csv, suntimes.csv.
data/processed/
data/preprocessed/
# QC plots (03_qc_plots.py) — regenerated HTML, not source.
outputs/
